# Site Rules

- This app is now a `Vite + React + Tailwind 4` project migrated from AI Studio, not Next.js.
- Keep app code under `src/`; use `pnpm dev`, `pnpm build`, and `pnpm preview`.
- Preserve the exported visual direction unless the user explicitly asks for a redesign.
